

(add-to-list 'load-path "~/.emacs.d/scala-mode")

(require 'scala-mode-auto)
(add-hook 'scala-mode-hook
          '(lambda ()
             (yas/minor-mode-on)
             ))
(provide 'init-scala-mode-setup)
